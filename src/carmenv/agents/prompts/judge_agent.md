# CarmenV Agent Prompt Placeholder

This prompt is intentionally stored separately from business logic. The first
CarmenV baseline does not call an LLM; future agents must return structured JSON
that can be stored and attributed.
